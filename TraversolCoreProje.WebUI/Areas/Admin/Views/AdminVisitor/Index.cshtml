@*@model List<ResultVisitorViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<div class="row">
    <div class="card">

        <h5 class="card-header">Bildirimler</h5>
        <div class="table-responsive text-nowrap">
            <a class="btn btn-primary" asp-action="CreateVisitor" asp-controller="AdminVisitor"><i class="bx bx-add-to-queue me-1"></i> Yeni Ziyaretçi</a>
            <table class="table my-2">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Ad</th>
                        <th>Soyad</th>
                        <th>Şehir</th>
                        <th>İlçe</th>
                        <th>E-Posta</th>
                        <th>Aksiyonlar</th>
                        <th>Detaylar</th>
                    </tr>
                </thead>
                <tbody class="table-border-bottom-0">

                    @if (Model is { Count: > 0 })
                    {
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@count</strong></td>
                                <td>@item.Name</td>
                                <td>@item.Surname</td>
                                <td>@item.City</td>
                                <td>@item.Country</td>
                                <td>@item.Mail</td>
                                <td>
                                    <a asp-action="Update" asp-controller="AdminVisitor" class="btn btn-warning" asp-route-dataId="@item.DataProtect"><i class="bx bx-edit me-1"></i> Güncelle</a>|<a asp-action="Delete" asp-controller="AdminVisitor" class="btn btn-danger" asp-route-dataId="@item.DataProtect"><i class="bx bx-trash me-1"></i> Sil</a>
                                </td>
                                <td><a asp-action="#" asp-controller="AdminVisitor" class="btn btn-info" asp-route-dataId="@item.DataProtect"><i class="bx bx-data me-1"></i> Detaya Git</a></td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr class="text-center">
                            <td colspan="8">Ziyaretçi Bulunamadı...</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

</div>*@
